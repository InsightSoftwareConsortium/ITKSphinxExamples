
add_example(ComposeVectorFromThreeScalarImages)
compare_to_baseline(EXAMPLE_NAME ComposeVectorFromThreeScalarImages
  BASELINE_PREFIX OutputBaseline
  )

add_example(CreateVectorImageFromScalarImages)
compare_to_baseline(EXAMPLE_NAME CreateVectorImageFromScalarImages
  BASELINE_PREFIX OutputBaseline
  )

add_example(JoinImages)
compare_to_baseline(EXAMPLE_NAME JoinImages
  BASELINE_PREFIX OutputBaseline
  )

add_example(ConvertRealAndImaginaryToComplexImage)
compare_to_baseline(EXAMPLE_NAME ConvertRealAndImaginaryToComplexImage
  BASELINE_PREFIX OutputBaseline
  )
