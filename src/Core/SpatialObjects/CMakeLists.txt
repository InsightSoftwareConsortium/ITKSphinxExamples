
add_example(ConvertSpacialObjectToImage)
compare_to_baseline(EXAMPLE_NAME ConvertSpacialObjectToImage
  BASELINE_PREFIX OutputBaseline
  )

add_example(Ellipse)
compare_to_baseline(EXAMPLE_NAME Ellipse
  BASELINE_PREFIX OutputBaseline
  )

add_example(LineSpacialObject)
compare_to_baseline(EXAMPLE_NAME LineSpacialObject
  BASELINE_PREFIX OutputBaseline
  )

add_example(Blob)
compare_to_baseline(EXAMPLE_NAME Blob
  BASELINE_PREFIX OutputBaseline
  )

add_example(ContourSpatialObject)
compare_to_baseline(EXAMPLE_NAME ContourSpatialObject
  BASELINE_PREFIX OutputBaseline
  )
